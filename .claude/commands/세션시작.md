아래 순서로 실행:

1. `git pull` 실행
2. 현재 시간을 `.claude/session_start.txt`에 `YYYY-MM-DD HH:MM` 형식으로 저장
3. `production/devlog/` 폴더에서 가장 최근 데브로그 파일을 읽어 지난 작업 내용과 다음 목표 확인
4. 다음 목표를 기반으로 오늘 할 작업을 구체적으로 제안하기
   - 단순 나열 금지. "오늘은 A부터 시작하는 게 좋겠습니다. 이유는 ~" 형식으로 제안
