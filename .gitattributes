# 줄바꿈 규칙 — 실행 스크립트는 OS에 맞는 줄바꿈이 필요하다
# (cmd 배치 파일은 LF만 있으면 오작동, 맥 .command 는 CRLF 가 있으면 오작동)
*.bat     text eol=crlf
*.cmd     text eol=crlf
*.vbs     text eol=crlf
*.command text eol=lf
*.sh      text eol=lf
